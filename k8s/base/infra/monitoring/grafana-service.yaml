apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: ums
  labels:
    app: grafana
    component: monitoring
spec:
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
