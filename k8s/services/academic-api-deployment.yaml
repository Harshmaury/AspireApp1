apiVersion: apps/v1
kind: Deployment
metadata:
  name: academic-api
  namespace: ums
spec:
  replicas: 2
  selector:
    matchLabels:
      app: academic-api
  template:
    metadata:
      labels:
        app: academic-api
    spec:
      containers:
      - name: academic-api
        image: aspireapp1-academic-api:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: ums-config
        - secretRef:
            name: ums-secrets
        env:
        - name: OTEL_SERVICE_NAME
          value: academic-api
        resources:
          requests:
            memory: 256Mi
            cpu: 250m
          limits:
            memory: 512Mi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 40
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
